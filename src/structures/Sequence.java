package structures;

public class Sequence
{
	private byte[] sequence;
	
	public Sequence(byte[] sequence) 	{ this.sequence = sequence; }
	
	public void 	setSequence(byte[] sequence) 	{ this.sequence = sequence; }
	public byte[] 	getSequence() 					{ return this.sequence; }
}
